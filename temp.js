let memory = []; // 1000
let mptr = 0;
let input = []; // 1000

let count;
print ("Enter the number of instructions: ");
scan(count);

let i = 0;
scan[count, input]

i = 0;
while(i < count) {
    let instruction = input[i]; 
    
    if (instruction == '>') {
        mptr++;
    } else if (instruction == '<') {
        mptr--;
    } else if (instruction == '[') {
        
    }

    i++;
}

